[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "netguard"
version = "0.1.0"
description = "NetGuard AI: A Machine Learning Network Intrusion Detection System"
readme = "nids-project/README.md"
requires-python = ">=3.11"
authors = [
    { name = "Developer" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Topic :: Security",
]
dependencies = [
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "scikit-learn>=1.3.0",
    "joblib>=1.3.0",
    "click>=8.1.0",
    "rich>=13.0.0",
    "textual>=0.40.0",
    "starlette>=0.27.0",
    "uvicorn>=0.20.0",
    "requests>=2.28.0",
]

[project.optional-dependencies]
train = [
    "xgboost>=2.0.0",
    "imbalanced-learn>=0.11.0",
    "shap>=0.43.0",
    "optuna>=3.4.0",
    "matplotlib>=3.7.0",
    "seaborn>=0.12.0",
]
dev = [
    "pytest>=7.0.0",
    "httpx>=0.24.0",
]

[project.scripts]
netguard = "netguard.cli:cli"

[tool.setuptools.packages.find]
where = ["nids-project"]
include = ["netguard*"]

[tool.setuptools.package-data]
netguard = ["models/*", "web/static/*", "web/templates/*"]
